namespace _Game.Scripts
{
    /// <summary>
    /// Identifies the type of attack the player has performed.
    /// Extend this enum to add more attack types as the game grows.
    /// </summary>
    public enum AttackType
    {
        Primary,
        Secondary,
        Special
    }
}
